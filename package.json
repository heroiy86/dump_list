{
  "name": "dump-list",
  "version": "1.0.0",
  "description": "A simple task management application",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "fs-extra": "^11.1.0",
    "gh-pages": "^5.0.0"
  }
}
